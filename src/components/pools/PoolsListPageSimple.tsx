import React from 'react'
import styled from '@emotion/styled'

const Container = styled.div`
  padding: 20px;
  color: white;
`

export default function PoolsListPageSimple() {
  return (
    <Container>
      <h1>Pools List (Simple Version)</h1>
      <p>This is a temporary simple version to test the routing.</p>
    </Container>
  )
}
